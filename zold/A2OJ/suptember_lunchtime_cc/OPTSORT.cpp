#include <bits/stdc++.h>

using namespace std;
int main()
{
    int t, n;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        cin >> n;
        vector<int> x(n), y(n);
        for (int j = 0; j < n; j++)
        {
            cin >> x[i];
        }
        }
    return 0;
}