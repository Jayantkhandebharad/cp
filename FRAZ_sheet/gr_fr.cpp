#include <bits/stdc++.h>
using namespace std;

void solve(int n, int m)
{
    vector<vector<int>> v(n, vector<int>(m));
    for (int i = 0; i < n)
}

int main()
{
    int t, n, m;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        cin >> n >> m;
        solve(n, m);
    }
}